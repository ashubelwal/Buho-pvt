import { LightningElement } from 'lwc';
import logoMeta from '@salesforce/resourceUrl/logoMeta';
import logoX from '@salesforce/resourceUrl/logoX';

export default class FooterComponent extends LightningElement {
    logoMeta = logoMeta;
    logoX = logoX;
}